# 更新记录（faculty-research skill / 教授名单批量补全 notebook）

这份文件记录了这个skill从最初版本到现在的完整改动历史。notebook本身只保留
"怎么用"的说明和实际要跑的代码，版本变更的详细原因和讨论过程都放在这里，
避免notebook被历史记录撑得越来越长、每次打开都要先划过一大段文字才能开始跑。

版本号从旧到新排列，notebook文件名的版本号（如`_v20.ipynb`）和这里的标题
大致对应，但不是严格一一对应——有些改动是纯脚本/文档层面的调整，不一定
每次都对应一次notebook文件重新发布。

---

## 本版本更新说明（v2）

针对反馈的问题：

1. **「当前机构」显示为空** —— 之前用的是 OpenAlex 原始 `last_known_institutions` 字段（这个字段经常是空的），现在改用有兜底的 `effective_institutions`。
2. **机构别名转换正确但仍判定"模糊匹配"** —— 之前只要目标机构在作者**一生任何时候**出现过就算通过，导致多个不同的人都"沾边"。现在改成：优先要求目标机构是该作者**最近一次**的隶属（或在 OpenAlex 的 `last_known_institutions` 里），历史上出现过但不是最近一次的，单独标记为 `possible_move`，一律转人工核实，不会被当成 ok 自动采信。
3. **合并逻辑收紧**（两轮修正）：
   - 第一轮：合并前要求候选记录独立满足"当前机构"，不再仅凭主题重叠合并跨机构记录。
   - 第二轮（本次）：现实中大部分"同一人被拆成两条 OpenAlex 记录"是因为任职信息更新后系统重建了新 entity。因此合并条件改为——**先看其中一条记录"最后一次任职"是否出现在另一条记录的任职历史里**（判断是否为同一条时间线的前后接续），**如果是，再要求固定字段（姓名全称、ORCID 如果都有的话）完全一致**才允许合并；只要有一对候选不满足，整组都不合并、转人工确认。主题重叠检查仍保留作为附加的安全网，但不再单独作为合并依据。
4. **Prof/Assoc 等职称前缀** —— 确认本来就已经归一化处理，不是前面问题的成因（顺手删掉了一段没被用到的死代码）。

另外顺带修了两个后面步骤会遇到的坑：`confidence.py` 打分脚本原来引用了两个从未定义的变量，跑到「可信度评估」那一步必崩溃；`merge_to_excel.py` 里可信度那几列的字段名和 `confidence.py` 实际输出对不上，导致这几列永远是空的。这两个也已修复。

新增一列 **`OA_机构时效性`**（current / historical / unknown），以及一个新状态 **`possible_move`**（目标机构仅为历史隶属，需人工确认是否已转校）。合并记录的 `merge_note` 字段现在会写明是依据哪条证据链合并的，方便回溯核查。


---
## v3 更新：单个 OpenAlex ID 内部身份污染检测

上一版的机构时效性检查、跨entity合并检查，都是"拿候选人之间互相比较"，如果从始至终只搜到一个候选人（比如 Wei Zhang 这个 case），根本没有"第二个候选"可比对，之前所有检查都会被绕过。

新增 `_contamination_risk()`：不比较候选人之间，而是看**这一个** OpenAlex ID 自己的论文主题（`topics`/`x_concepts` 的 domain 分类）是否横跨了明显不相关的学科（例如医学 + 计算机同时出现），命中就在 `match_method` 后面追加 `_profile_contamination_risk`，`batch_enrich.py` 会把这类记录强制标记为 `needs_review_contaminated`，即使它是唯一候选、机构时效性也验证通过，也不会被当成 `ok` 自动采信。


---
## v4 更新

1. **`--field` / `--since` 不再硬编码**：配置区（第3步之后）新增下拉框选领域（`FIELD`，Colab 表单里可直接下拉选），`SINCE` 年份也做成了可改的表单输入，运行匹配的两处命令改为读取这两个变量，不再是写死的 `"finance"` / `"2022"`。
2. **新增"单机构 vs 多机构混杂"启发式**：候选池里如果恰好一个候选人只对应 1 个机构、其余候选人都对应 3 个以上机构，且这个"干净"候选人自己的论文主题没有跨越明显不相关的学科（复用了身份污染检测的逻辑判断"是否集中"），直接判定为目标人选（`matched_on_current_institution_clean_profile_heuristic`），不再需要 `field_score` 关键词命中——实测中候选人的论文主题经常不会直接包含 `finance` 这类词表词汇，原来单纯靠关键词打分打不破平局。
3. **新增两个统计列**：`OA_发表论文总数(历史总计)`（OpenAlex 记录的作者历史发表总数）与 `OA_近年发表数`（原来叫"近三年"，其实取决于 `--since` 设的年份，容易误导，已改名）。
4. **新增「论文明细」工作表**：最终导出的 xlsx 里现在有两个 sheet——原来的名单 sheet，加一个新的「论文明细」sheet，每篇论文一行，包含：教授姓名、名单机构、论文标题、发表年份、期刊/会议、**该作者本人在这篇论文上的所属机构**（不是他现在的机构，是发这篇文章时的机构）、文章链接、DOI、合作者。

**关于被引数和近年文章的确认**：`OA_被引总数` 是 OpenAlex 给出的作者**历史总被引**，不是近年窗口内的被引数（目前没有单独统计"近年被引数"，如果需要可以再加）；`OA_近年发表数` 对应的年份范围由配置区的 `SINCE` 决定，不是固定"2022年之后"或"近3年"，改 `SINCE` 的值就能调整。


---
## v5 修复：机构别名缓存"学了但没记住"的 bug

之前反馈"机构一致性检查用的是简单子串匹配"，往下查发现根因有两层：

1. `merge_to_excel.py` 是完全独立的脚本，压根没 `import resolve_v2`，最后那个"机构是否一致"的核对是我另外重写的一套弱子串匹配，没有复用前面已经做好的别名表基础设施。**现已修复**：`merge_to_excel.py` 改为 `import resolve_v2 as rv`，直接调用 `resolve_institution()` 做核对，缩写会先转换成 OpenAlex canonical 全称再比较。

2. 更深一层：`resolve_v2.py` 里 `_save_alias()`（负责把新解析出来的机构写回 `institution_aliases.json`，讲道理这样以后同一个机构就不用重新联网搜了）里有一行判断逻辑写反了——`if key in d and not d[key].get("learned", True): return`。种子表里预置的 HKU / NTU / HKUST 这些条目本来就是 `"learned": false`（表示"还没配上 openalex_id"），但这行代码的效果恰恰是：**只要条目已经在文件里且标记是 `learned: false`，就直接放弃写入**——正好把最需要被补全的那批条目锁死了，导致这几个机构每次都得重新联网搜索，`openalex_id` 永远填不进去。已删除这条多余且写反的保护逻辑（这个函数本来就只会在"之前没有可用 id"时被调用，不存在会覆盖已验证结果的风险）。

用模拟数据验证过：修复后 `resolve_institution("HKU", ...)` 第一次成功解析后会把 `openalex_id` 真正写回磁盘上的 `institution_aliases.json`；`merge_to_excel.py` 作为全新子进程运行时（和 notebook 里 `batch_enrich.py` → `merge_to_excel.py` 两步分别 `subprocess.run` 的执行方式一致）能读到这个缓存，之前"HKU vs University of Hong Kong"被误判为机构不一致的问题不再出现。


---
## v6 修复：干净候选启发式被自己的一个残留条件误伤

上一版把"单机构 vs 多机构混杂"启发式的判断依据从"关键词命中 `field_score`"改成了"学科是否集中（复用污染检测）"之后，函数内部已经不再用到 `field` 这个参数——但调用它的地方忘了把 `if field and len(work_pool) > 1:` 这个已经过时的门槛去掉。结果是：只要调用时 `field` 是空字符串或 `None`，这条启发式会被整个跳过，哪怕候选人的机构数据已经足够清晰地判断出来了。已改为 `if len(work_pool) > 1:`，不再要求 `field` 非空。

用模拟数据复现过：`field=""` / `field=None` 时旧代码确实停在 `ambiguous_same_institution`（和反馈的现象一致），`field="finance"` 时旧代码反而是正常的；修复后三种情况全部正确解析到干净候选人。


---
## v7：全量代码梳理，清掉死代码，堵住几个"新状态没被旧逻辑感知到"的漏洞

按要求把 `.skill` 里所有脚本过了一遍，找已经被取代但没删的旧代码，以及新增状态后忘记同步更新的旧判断逻辑：

**删掉的死代码**（定义了但确认没人调用）：
- `resolve_v2.py`：`_inst_ok`、`_topics_of`（分别被更完善的 `_affil_ids`/`verify_institution`、`_topic_set` 取代）
- `confidence.py`：`_inst_tokens` 及其专用的 `ALIASES` 导入（已被 `_inst_agrees`+`same_institution` 取代）；`METHOD_BASE` 里三个 `resolve_v2.find_author` 现在永远不会返回的旧字符串
- `fetch_openalex.py`：独立维护的弱版 `find_author()`（不做机构ID解析/时效性判断/污染检测，功能被 `resolve_v2.find_author` 全面取代）——`main()` 改为直接调用 `resolve_v2.find_author`
- `batch_enrich.py` / `merge_to_excel.py`：`rejected_field_mismatch` 这个从未被实际产生过的状态分支

**修的"新状态没被旧逻辑感知到"**：
- `openalex_links.py` 生成"待人工确认.md"时默认只筛 `--status ambiguous` 前缀，新加的 `possible_move`/`needs_review_contaminated` 状态因为前缀不同，之前会被漏掉。现在默认改成"非 ok 就都算"，和 `merge_to_excel.py`/`batch_enrich.py` 结尾汇总的判定口径统一。
- `batch_enrich.py` 结尾打印"需要人工复核"名单时也只筛了 `ambiguous`，同样漏掉了新状态，一并修了。
- `confidence.py` 里"近三年论文加分"的判断硬编码成了 `>= 2023`，会随时间推移逐年过时，改成基于当前日期动态计算。

**消除重复维护、容易漂移的数据**：
- `resolve_identity.py`（独立的单人查询工具）原来调的是 `fetch_openalex.py` 那个已删除的弱版 `find_author`，会直接报错——已改为调用 `resolve_v2.find_author`，和批量流程用同一套解析逻辑，保留了它原有的 ORCID 交叉验证。
- `reverse_lookup.py` 原来自己维护一份 `FIELD_TOPICS` 领域关键词表，和 `resolve_v2.FIELD_HINTS` 已经有几处轻微不同步（比如 "om" 领域一个有 "operations research" 一个没有），现在改为直接复用 `resolve_v2.FIELD_HINTS`，避免以后改一处忘另一处。

全部改动在隔离的沙盒环境里重跑了此前建立的回归测试（干净候选启发式、拆分实体合并判定、身份污染检测、field 为空场景、机构别名一致性检查），结果与预期一致，未引入功能性回归。


---
## v8 修复：干净候选启发式用"全职业生涯机构数"当门槛，几乎打不到任何真人

上一版"单机构 vs 多机构混杂"启发式判断机构数用的是 `_affil_ids()`——这个函数统计的是候选人**一辈子出现过的所有机构**，不是候选列表里实际展示的那份"机构=[...]"（那份显示的其实是 OpenAlex 的 `last_known_institutions`，即当前/近期任职）。

问题是：一个真教授只要博士不是在现在这家机构读的，`_affil_ids()` 就会算出 2 个机构（母校 + 现在的学校），永远够不到 `<=1` 的"干净"门槛——这个 bug 对几乎所有有正常职业履历的人都成立，不是个别情况，所以启发式实际上几乎从来没真正生效过。

改成用 `_current_institution_names()`——和候选列表实际展示给你看的那份数据同一个来源（`last_known_institutions`，为空则退回 `affiliations` 列表）。同时注意到一个连带问题：`_fmt` 展示用的这份数据本来就把 `affiliations` 兜底截到前 3 条（纯粹为了打印好看），如果计数也复用这个截断，"混杂"候选人的机构数永远算不出超过 3，`noisy` 判定又会失效——所以计数时特意不做这个截断，只有展示时才截。

用一个更贴近真实情况的场景验证过：候选人博士期间在另一所学校、现在只在 HKU 任职（`last_known_institutions` 只有 HKU 一条，但完整履历里有 2 个机构）——修复前这种情况会被误判为"不干净"而跳过启发式，修复后能正确识别。同时重跑了之前建立的全部 6 项回归测试，结果都和预期一致。


---
## v9：邮箱做成可填变量 + 一轮运行时逻辑漏洞排查

**MAILTO 也做成表单变量**：和 SINCE/FIELD 一样用 Colab 的 `#@param` 语法，配置区可以直接改，不用去 6 处代码里找哪里写死了邮箱（顺手把 SHEET 也改成了文本输入框）。

**排查中发现并修复的运行时问题**：

1. **缓存 key 没带上 `--field`/`--since`** —— `batch_enrich.py` 判断"这个人是不是已经处理过"只看姓名+机构，换个领域或改个起始年份重新跑，会直接命中旧缓存、静默返回上一次的结果。现在 key 里加上了这两个参数；另外加了 `--no-cache` 选项，不用手动删缓存文件也能强制重新拉取。

2. **API 请求没有重试** —— 遇到限流（429）或偶发 5xx，之前是直接抛异常，而大多数调用方是 `try/except: continue`，相当于静默丢弃这次搜索，可能让本来能解析出来的人被误判成 `not_found`，且完全没有提示。现在 `resolve_v2.py` 和 `fetch_openalex.py` 的请求函数都加了指数退避重试。

3. **`merge_to_excel.py` 按姓名回填结果时没带机构** —— 如果名单里有两个同名不同机构的人（常见姓氏很容易撞上），后一个人的数据会静默覆盖前一个，导致两行都拿到同一份、其中一份是错的数据。现在改成按"姓名+机构"组合匹配，且用模拟的同名不同机构数据验证过不再互相覆盖。

4. **最后一步没传 `--mailto`** —— notebook 里生成最终 xlsx 那一步调用 `merge_to_excel.py` 时漏了 `--mailto` 参数，导致机构一致性检查兜底联网解析时没走"礼貌池"，容易被限流。已补上。

5. **`per_page` 应为 `per-page`** —— `fetch_openalex.py` 的 `works()` 用的是 Python 关键字参数 `per_page=`（下划线），但 OpenAlex API 实际认的参数名是 `per-page`（连字符）。因为关键字参数不能带连字符，这个参数其实从来没真正发出去过，OpenAlex 会静默忽略、退回默认的每页 25 条——**发表论文超过 25 篇的教授，论文明细和近年论文数量一直在被默默截断**。已改成和代码库其余位置一致的写法，用模拟请求验证了实际发出的参数确实带上了 `per-page: 200`。



---
## v10 更新：新增「JCR期刊分档 + 代表作推荐」功能，清理配置区死代码

**新功能**（详见 `references/journal-ranking-design.md`）：给每位教授的近年论文做 JCR
期刊质量分档（①高质量期刊作者 / ②成果较好 / ③不建议 / ④默认），并从中挑最多3篇代表作。
需要你另外上传一份 JCR 期刊名单 xlsx（版权数据，不随本项目分发，见下方第 3 步）；不上传也
完全不影响原有的身份解析流程，只是不会生成分档结果。

核心设计：
- 两个时间窗口分开配置——`WINDOW_A_SINCE`（默认2020，判断"是否有高质量期刊"）和
  `WINDOW_B_SINCE`（默认2023，真正挑代表作的窗口），彻底替换掉旧的单一 `SINCE`。
- 期刊名匹配分三层：JCR全称精确匹配 → JCR"期刊缩写"列精确匹配（经全量核查，这一列在
  22643条记录里覆盖率100%且无重复，是很干净的第二精确匹配层）→ 模糊匹配兜底。匹配不上的
  论文明确标注"未匹配"，不会被静默当成最好或最差。
- ESCI 期刊百分位统一减25分再参与判档；UTD24 期刊直接判①档，不看数字百分位。

**清理的死代码/重复代码**：
- 配置区的 `SINCE` 已被拆分为 `WINDOW_A_SINCE`/`WINDOW_B_SINCE`，不再是同一个变量在两处
  含义不同的地方复用。
- 原来「第8步：可信度评估」重复出现了两次（一次输出到 `enriched_scored.json`，另一次直接
  覆盖 `enriched.json`），两套逻辑并存容易让人不确定该跑哪个、也浪费一次不必要的子进程调用。
  现在只保留一条干净的链路：可信度评估统一读取"期刊分档跑过则用 ranked.json，否则用
  enriched.json"，输出到 `enriched_scored.json`，后面生成 Excel 那一步固定读这一个文件。
- `batch_enrich.py` 的 `--since` 参数改名为 `--window-a-since`（含义不变，只是名字和新的
  窗口体系对齐），避免同一个 flag 在 notebook 不同版本里改来改去。

---
## v11 更新：代表作排序改按JCR百分位/排名，不再直接比影响因子（JIF）；补全匹配审计信息

**修复的问题**：`pick_representative_papers()` 原来按论文所在期刊的原始影响因子
（JIF数值）从高到低排代表作。这是个真实bug——JIF的量纲在学科间差异巨大（医学期刊
JIF常见50-600，金融顶刊通常只有5-15），直接比JIF数值会让医学期刊论文系统性地排到
金融顶刊论文前面，这和"论文相对自己学科有多顶尖"是两回事。用真实数据验证过：
Journal of Finance（UTD24，IF≈12）vs CA-A Cancer Journal（非该教授所在学科，
IF≈685）——按旧逻辑后者会排第一，明显不合理。

**新逻辑：优先级分档（priority_tier），逐级判断，"插队"必须有 stated reason**：

| 优先级 | 条件 | 原因 |
|---|---|---|
| 0 | 命中 UTD24 | 分档时UTD24已经覆盖数字百分位，选代表作保持同一套逻辑 |
| 1 | 学科类别内排名前3（如 3/109） | 排名比百分位更敏感，能分辨"大类目里前几名"这种百分位分辨不出的差异 |
| 2 | 学科类别内排名前5 | 同上，门槛放宽 |
| 3 | 已匹配JCR，无特殊排名 | 按有效百分位（ESCI降级后）排序，可跨学科比较 |
| 4 | 未匹配到JCR | 只在候选不足3篇时兜底 |

每篇代表作现在都带 `priority_reason` 字段，写进最终xlsx"代表作推荐"表的
"排序理由(是否插队)"列，小样本预览里也会打印。详见
`references/journal-ranking-design.md` 第7节。

**同时修的一个可核对性问题**：之前"依据"文案只引用论文标题，但那篇论文很可能不在
预览打印的前12篇里，且如果是靠期刊缩写/ISSN/模糊匹配这几层命中的，JCR里真正匹配到
的期刊名本来就和OpenAlex给的venue原文不一样——拿venue原文去JCR表搜，当然搜不到。
现在①②③档判断依据都会带上完整的"引用论文"清单（venue原文 + JCR匹配到的规范期刊名
+ 匹配方式 + 分区/百分位/UTD24），小样本预览和最终xlsx（"论文明细""代表作推荐"两个
sheet都新增了"JCR匹配名"列）都能直接核对。

**UI 精简**：`MAILTO` 重命名为 `EMAIL`（Colab表单的框标题就是变量名本身）；`FIELD`
和 `FIELD_MAJOR_CUSTOM` 改成普通代码变量、不再显示成表单框（想改值直接编辑代码里的
这一行即可，不影响其余流程）。

---
## v12 更新：跨学科身份污染检测——一位教授不该同时发表金融和肿瘤医学论文

**背景**：寻找导师的学生通常集中在一个领域，一位教授的窗口A论文里如果同时出现
"金融"和"肿瘤医学"这类明显不相关的学科，对真实学者的正常发表记录而言基本
不可能——这大概率是OpenAlex把另一个同名人的成果错误合并进了这个作者ID
（`resolve_v2.py`身份解析阶段已经在查的"profile contamination"问题的同一种
失败模式，这次是从JCR学科类别这个独立角度再查一遍，两道检测网互不替代）。

新增 `detect_domain_contamination()`：把JCR的254个学科类别用关键词粗分成7个
大类（商科/经济/管理、医学/临床、生物/化学/农业、物理/天文/材料/工程、
计算机/数学/统计、社会科学/心理/教育/法律、地球/环境科学），如果一位教授
窗口A的论文跨越了≥2个大类，就：
- 在分档依据（`tier_reason`）最前面加上 ⚠️ 警告，说明具体跨了哪几个大类；
- 小样本预览额外打印一段醒目提示，附上该作者OpenAlex主页链接方便直接核实；
- 全量跑完后终端汇总打印"疑似身份污染教授清单"；
- 最终xlsx的教授名单sheet新增 `JR_疑似身份污染(需人工核实)` 列，一眼就能筛出来。

刻意设计成**只提示、不拦截**——不会改变分档结果或代表作推荐，只是提醒先去
OpenAlex核实身份再采信。用真实案例验证过：Journal of Finance + CA-A Cancer
Journal（IF≈685）这种组合会被正确标记；纯金融领域的正常论文组合不会被误判。
详见 `references/journal-ranking-design.md` 第8节。

---
## v13 更新：全量跑几个小时看起来"卡住"的真正原因——不是脚本卡了，是看不到进度

**问题排查**：有用户反馈全量跑了2小时没反应，最后手动中断。查下来
`batch_enrich.py` 其实一直在正常打印每个人的处理进度（`[152/312] ok 教授姓名`），
但 notebook 里跑它用的是 `subprocess.run(cmd, capture_output=True, text=True)`
——这个写法会把所有输出攒到进程**完全结束**才一次性吐出来，几个小时的运行期间
屏幕上完全是空的，没有任何办法知道是在正常跑、跑到哪了、还是真的卡住了。

**修复**：
- 新增一个 `run_streaming()` 小工具（第5步载入脚本后面那一格），用
  `subprocess.Popen` 实时逐行打印输出，不再等进程结束才倒出来。第6/7/9/10/11/12
  步和附录的反向查找，全部换成用这个工具跑，现在能实时看到进度了。
- `batch_enrich.py` 增加了每处理20人打印一次"已用时/预计还需多久"的粗略ETA。
- 之前只有**缓存**（`.openalex_cache.json`）每25人存一次盘，最终的 `enriched.json`
  只在全部跑完之后才写一次——这意味着中途被打断的话，哪怕缓存保住了大部分进度，
  也完全没有 `enriched.json` 可用，等于那几个小时的运行本身白跑（缓存能让重新
  跑的时候秒过已处理的人，但你手头没有任何可以先用的文件）。现在改成每25人也
  同步覆盖写一次 `enriched.json`，中途断了也有部分结果能先用。

**如果你之前那次2小时的运行是在同一个 Colab 运行时里跑的、还没重启**，
`.openalex_cache.json` 大概率还在——直接重新运行第7步，已经处理过的人会
从缓存秒读，不是从头再来。

---
## v14 更新：真实261人名单跑出来的几个真bug——不是"没优势"，是这几处没做对

用户反馈"很多教授查不到、同一教授学科跨度过大"，质疑整套方案不如换成Semantic Scholar。
逐条查了实际输出，找到并修了3类真实存在的问题（不是猜测，每条都用真实/构造数据验证过）：

**1. NOT_FOUND 掺了"API请求失败"进去，虚增了查不到的人数**

`resolve_v2.py`里所有OpenAlex请求都套了一层`except Exception: continue`，请求失败和
"OpenAlex真的没这个人"在输出里长得一模一样，全都显示`not_found`。现在改成：统计每个
人的请求"尝试次数"和"失败次数"，如果**全部**请求都是因为异常失败（不是正常返回0条），
标成`api_error`而不是`not_found`——这两种情况处理方式完全不同：`not_found`该去人工核实，
`api_error`该直接重跑（这些人没写入缓存，重新运行会自动只重试这些人，不影响已成功的）。
`batch_enrich.py`最后会单独列出"这些人重新跑一遍即可"的清单。

**2. 跨学科身份污染检测里，"物理/工程"桶的关键词太宽，把不相关类目也扫了进去**

审计了JCR全部254个学科类目实际分桶结果后，找到几个真实的误分类：
- `"COMPUTER SCIENCE, SOFTWARE ENGINEERING"`（计算机类目）因为含"ENGINEERING"这个词，
  被错误分进了物理/工程桶
- `"GEOCHEMISTRY & GEOPHYSICS"`（地球科学类目）因为"CHEMISTRY"这几个字母嵌在词中间被
  子串匹配命中，被错误分进了生物/化学桶
- `"PHYSICS, ATOMIC, MOLECULAR & CHEMICAL"`（物理类目）因为含"MOLECULAR"，被错误分进
  了生物桶
- `"CELL & TISSUE ENGINEERING"`、`"AGRICULTURAL ENGINEERING"`同理被"ENGINEERING"
  误伤

这几个bug直接导致"一个教授同时横跨5个学科"这种警告可能压根不是真的身份污染，只是
分类逻辑本身把不相关的类目名错误地拆进了不同的桶。修复方式：① 关键词匹配改成要求
词首有边界（避免"CHEMISTRY"这种词被嵌在"GEOCHEMISTRY"中间误伤）；② JCR类目命名习惯
是"主类目, 子类目"格式，改成优先用逗号前的主类目判断，查不到再退回查整串——这样
"COMPUTER SCIENCE, SOFTWARE ENGINEERING"会优先按"COMPUTER SCIENCE"分类，不会被后面
提到的"ENGINEERING"抢走。修完之后重新核对了全部254个类目的分桶结果，确认没有引入新的
误判。

**这两类问题都不是"OpenAlex不如Semantic Scholar"能解决的**——它们是这套代码自己的
bug，换数据源不会让bug消失。查资料确认了一点：Semantic Scholar自己的机构关联信息是
自由文本，没有OpenAlex那种ROR机构ID体系，`matched_on_current_institution_verified`
这套机构验证机制没法照搬到S2上，贸然全量切换大概率会让"匹配到错的人"这类问题更严重，
不是更轻。

**没有改、留作已知局限的**：`ambiguous_same_institution`（2个候选选不出来）目前还是
路由到人工核实，没有进一步尝试自动消歧——比起冒险猜错人，宁可让人工确认的人数多一些。

---
## v15 更新：请求失败拖2分钟的问题、以及用"学科不可能同时出现"这条信号自动收窄需要人工核实的候选人

**1. 请求失败为什么会卡快2分钟**

一个人的身份查找最多要发起8-10次OpenAlex请求（机构过滤×2种方式×最多4个姓名变体，
加上兜底的纯姓名搜索）。之前每一次请求失败都会独立走完整的4次重试退避
（1.5+3+6+12秒），如果OpenAlex当时正好不稳定，同一个人的好几次请求全部各自重试到底，
累计起来就是截图里看到的那种"卡快2分钟"。

修复：加了一个"降级"标记——只要这次查找里已经有一次请求完整重试失败过，同一个人
后续的请求就不再重复整套4次退避，直接快速失败（最多1次尝试，不睡眠）。实测：全部
请求都失败的最坏情况下，从原来的（理论上）几分钟降到约21秒。顺手还去掉了一个之前
就存在的小浪费——重试的最后一次失败后，代码原来还会白白多睡12秒（反正后面也不会
再试了）。

**2. 用"学科不可能同时出现"这条信号自动排除掉候选人里明显不对的那个**

`resolve_v2.py`本来就有一套污染检测（`_contamination_risk`），但只用了"医学 vs
非医学"这一条粗规则。现在扩展成更完整的"硬科学 vs 商科/金融/经济/社科"对照表，
基于OpenAlex自己的26个学科Field分类（比4大类Domain细得多，专门去查证过官方文档
confirmed）：

- **物理、化学、生物、医学**这类硬科学 vs **商科、金融、经济、社科**：两边同时出现
  判定为高风险（一个人真实研究横跨这两边的可能性极低）
- **计算机、数学、工程、环境科学、心理学**故意不放进两边任何一边——这些是合理的
  "桥梁学科"（金融科技、计算金融、健康经济学、运筹学都是真实存在的正常职业路径），
  和商科/金融同时出现不该被当成污染信号

关键的是，这条信号现在不只是"事后打标签"，而是直接用来**收窄需要人工确认的候选人
范围**：如果`ambiguous_same_institution`（两个候选人绑在一起选不出来）里，其中一个
的学科组合明显不可能（比如金融+肿瘤医学），另一个学科单一、正常，会自动排除掉那个
不可能的，只剩一个的话直接选中——不再需要人去看这两个候选人再做判断。如果两个候选人
都"干净"或者都"有问题"，还是老老实实交给人工核实，不会瞎猜。

用构造数据完整测试过：①金融+物理 / 金融+医学 / 生物+金融——判定为污染；②金融+计算机
/ 纯"健康经济学"（属于经济学范畴，没沾生物医学）——不判定为污染；③模拟一个真实的
ambiguous_same_institution场景（一个候选人干净、一个金融+肿瘤医学），确认能自动选中
干净的那个；④模拟两个都干净的候选人，确认依然正确保留为人工核实，没有强行猜。

**调试过程中发现并修正了自己刚引入的一个bug**：一开始把"粗粒度4类Domain"和"细粒度26类
Field"两种信号混进同一个集合里计数，导致合法的"金融+计算机"组合被误判成污染（因为
每个topic会同时贡献一个domain名和一个field名，人为把计数放大了一倍）。拆成两个独立
集合分别计数后修复，重新测试确认"金融+计算机"不再被误伤。

---
## v16 更新：两层身份污染检测的措辞统一；以及一次文件完整性排查

**措辞统一**：`journal_ranking.py`（基于JCR期刊学科的检测）和`resolve_v2.py`
（基于OpenAlex自身学科分类的检测）是两套独立机制，之前分别在不同地方报警，
用户容易误以为是两个不相关的系统。现在统一成"第一层/第二层"的说法：能走到
`journal_ranking.py`这一步、被它标记出问题的记录，按定义已经通过了`resolve_v2.py`
阶段的第一层检查（否则早被拦在`needs_review_contaminated`，根本不会往下走）——
所以`journal_ranking.py`发现的是第一层漏掉的情况，两者是互补关系，不是重复劳动。
分档依据文案、Excel列名（`JR_第二层身份核查疑点`）、`OA_机构时效性`列里第一层的
提示，现在用词一致，能让用户看出这是同一套体系的两层检查。

**一次文件完整性排查**：整理这一版之前，发现`resolve_v2.py`和`batch_enrich.py`
磁盘上的内容比交付给用户的v15 notebook里多出一段代码（一个"连续api_error熔断
冷却"机制、一个"expected_field感知的污染检测"扩展），但没有任何工具调用记录能
对应这段改动是我写的。这两段代码单看逻辑是合理的、和这个项目其他部分的设计思路
也一致，但既然对不上任何可追溯的操作记录，就不能不明不白地当成"我验证过的工作"
直接交付——所以这一版把这两个文件恢复成了和v15 notebook完全一致、经过实测的
版本，没有把那段代码带进来。

如果"连续几个人都请求失败就先冷却一下，不要一个个硬撑重试"这个思路你觉得有用，
可以专门说一声，我会重新老老实实实现、测试之后再加进去，而不是这样不清不楚地
出现在交付物里。

---
## v17 更新：身份可信度是推荐的前提，不是推荐之后顺带一提的备注

**修的问题**：之前的顺序是"先算出①②③④分档和推荐代表作 → 再在旁边提一句其实
身份可信度存疑"——这个顺序反了。这版把它改成：**先查第二层身份核查，有问题就
直接不推荐、说明原因；没问题才继续往下算分档和代表作**，逻辑上和打印顺序上都是
"身份 → 污染核查 → 推荐"这个顺序，不再是算完推荐才回头说身份有问题。

具体改动：
- `journal_ranking.py`：一旦第二层核查发现窗口A论文跨越明显不相关学科，直接跳过
  `pick_representative_papers`，不再往下算这个疑似被污染的身份该推荐哪几篇论文——
  最终xlsx里这类记录的"代表作推荐"表会显示0篇，选取依据写明原因。
- 全量跑完的终端汇总：污染警告清单挪到了分档结果统计前面打印。
- 小样本预览：同一个人的输出里，先查污染有没有问题，命中就直接打印警告+核实
  建议然后跳到下一个人，不再继续往下打印那些其实不会被使用的分区细节和推荐——
  没命中才继续正常显示分档依据、逐篇分区、推荐代表作。

**关于"跑三个独立代码段"**：把小样本试跑里"读取前10行生成test10.xlsx"和"跑
batch_enrich"这两个之前分开的格子合并成了一个，减少了一次点击/等待。身份识别
（batch_enrich）和期刊分档+推荐（journal_ranking）这两步本身有先后依赖关系
（要先知道是谁、有哪些论文，才能查期刊质量、算推荐），没办法合并成一次脚本调用，
但现在至少确保了：每一步该看的信息按正确顺序出现，不会出现"先看到推荐结果、
后来才发现这人身份存疑"这种情况。

---
## v18 更新：桥梁学科不算混乱；改成不阻断推荐的软提示

**背景**：v17把"第二层身份核查"设计成命中就直接跳过代表作推荐。但这个设计本身
有问题——金融系教授在信息系统、统计学这类期刊上发文是完全正常的学术生涯，不是
不同人的成果被误合并，之前的逻辑会把这种情况错当成"身份存疑"而不推荐，属于误伤。

**改动**：把这部分从"疑似身份污染检测"重新定位成"研究方向偏移提示"——
**只提示，不阻断**：

1. **桥梁学科豁免**：计算机/数学/统计这类学科和商科/金融同时出现，直接不算
   混乱（不管占比多高），因为这是常见的正常组合（金融科技、计算金融、信息系统）。
2. **任职机构数宽容度**：如果这位教授完整职业生涯只待过≤3个机构（用的是
   OpenAlex的完整机构历史`affiliation_institutions`，不是当前机构），说明这是
   一个连贯的单一学术生涯，触发提示的比例阈值从50%放宽到65%。
3. **不再阻断代表作推荐**：排除桥梁学科后，如果非主领域论文占比仍达到阈值，
   现在只是在`journal_ranking`字典里附加`domain_mix_note`字段、在分档依据前面
   加一句提示——代表作推荐照常进行，不会因为这条提示而跳过。是否要在意这条
   提示，判断权交给看结果的人，代码不替用户做这个决定。

相应地，最终xlsx的列名从"JR_第二层身份核查疑点(需人工核实)"改成
"JR_研究方向偏移提示"；小样本预览和全量汇总打印的提示措辞也从"⚠️疑似身份污染"
改成"💡研究方向可能有较大偏移"，语气上和真正需要人工核实身份的情况区分开。

用真实JCR数据验证过：①金融+统计学（桥梁学科）50%占比，不触发提示；②金融+
肿瘤医学（非桥梁）50%占比、机构数=5，触发提示；③同样50%占比但机构数=2（阈值
提到65%），不触发；④机构数=2但占比达到57%（超过65%阈值），触发；⑤全程验证
代表作推荐都正常出结果，不会被这条提示挡住。

---
## v19 更新：主领域判定改用FIELD_MAJOR锚定，加两条防误判过滤规则

**问题**：之前"研究方向偏移提示"里的主领域，是单纯按窗口A论文数量投票决定的
——票数最多的学科大类就是"主领域"。这在论文数少、或者票数接近时很不稳定；
更严重的是，如果一位教授碰巧在窗口期里，某个不相关学科的论文数量超过了他
真正专精的领域（完全可能发生，比如某几年合作发得多），投票法会把他真正的
领域当成"偏离"，把误判学科当成"主领域"——方向整个反了。

**改动**：

1. **主领域优先用`FIELD_MAJOR`锚定**：如果用户在配置里填的`FIELD_MAJOR`能
   匹配上窗口A论文实际出现过的某个学科大类，直接把那个大类定为主领域，不再
   看论文数量投票——这是用户对这批roster的先验知识，比单篇论文的分类投票
   靠谱。只有`FIELD_MAJOR`没填、或匹配不上时，才退回投票法兜底。
2. **最小样本量门槛**（≥4篇匹配JCR的论文才评估）：论文太少时"主领域是什么"
   本身答不清楚，不如不给提示。
3. **单篇噪音过滤**（非主领域大类下至少2篇论文才计入偏离比例）：单篇论文
   更可能是JCR分类噪音（比如"Multiple"多学科期刊选错了类别），而不是真的
   存在第二个研究方向。

**顺带的输出改进**：`domain_mix_note`的示例论文从"每类存1篇"改成"存该类下
全部符合条件的论文"，并且真正接进了Excel提示列和小样本预览的显示里（之前
这个信息算出来了但没地方展示）——现在提示文字不只是一个百分比数字，还会
带上具体是哪几篇论文，方便一眼判断是不是分类噪音。

用构造数据验证了FIELD_MAJOR锚定确实能修正投票法的方向性错误：3篇肿瘤医学+
2篇金融论文，不给FIELD_MAJOR时投票法把肿瘤医学错判成主领域（完全反了，
不会给出提示）；给了`--field-major "Business, Finance"`后，正确识别商科为
主领域，医学论文的60%占比被正确标记为偏离。全程验证代表作推荐依然正常出
结果，不受这条提示影响。

同步更新了`references/journal-ranking-design.md`第8节、`pitfalls.md`新增
第17条、`README.md`和`SKILL.md`里过时的"3层匹配"表述（应为4层，之前漏更新
ISSN那一层）。
---
## v20 更新：研究方向信号彻底重做——从"一条比例阈值"改成"不成立/少见/桥梁"三档独立规则

这是这个功能目前为止改动幅度最大的一次，起因是用户指出：桥梁学科和"医学+金融"
这种根本不可能的组合，不该走同一套"比例阈值"逻辑判断——应该是三个并列的分支，
各自有独立的触发条件。

### 学科分类：从7个粗粒度大类拆到17个

按学科惯例把原来过粗的几个大类拆开：

- 医学/临床 → 拆出**神经科学**（神经经济学是真实交叉学科）
- 生物/化学/农业 → 拆成**化学**、**生物学/生命科学**、**农业/食品科学**
- 物理/天文/材料/工程 → 拆成**物理/天文**、**材料科学**、**工程**（天文物理和材料
  科学的学科气质差异较大，工程和其他硬科学的桥梁关系也不一样）
- 计算机/数学/统计 → 拆成**计算机科学**、**数学/统计**（数学统计和金融的桥梁关系
  比纯计算机科学更紧密，比如计量经济学）
- 社会科学/心理/教育/法律 → 拆成**心理学**、**社会学/政治学/传播学**、**法学**、
  **教育学**、**人文**（心理学和商科的桥梁关系明显强于法学/人文，混在一起判断力
  损失最大）

商科/经济/管理和地球/环境科学保持不拆（内部关联度高/子领域和其他学科的桥梁关系
基本一致，拆了没有额外判断力）。

### 关系判断：家族默认值 + 具体例外，不是17×17全部手打

17个大类分到5个家族（A商科、B硬科学、C量化、D人文社科、E地球环境），先定
"家族×家族"的默认关系（比如B×A默认不成立、C×A默认桥梁），再对具体不符合家族
默认的学科对写例外（比如商科×神经科学，家族默认不成立，例外改成桥梁，因为
神经经济学是真实存在的领域）。这样维护量是"5×5+一份短例外清单"，不是136对
全部手工判断，以后学科继续拆分也不会让维护量爆炸。

具体的家族默认表和全部例外，见`references/journal-ranking-design.md`第8节。

### 三档独立触发规则

| 关系 | 触发条件 | 处理 |
|---|---|---|
| ❌不成立 | 该关系下论文数>1篇 | **整体不分档、不推荐**，建议人工核实身份 |
| ❌不成立 | 该关系下论文数=1篇 | 把这1篇（连带整个学科桶）**排除**，不参与分档和推荐判断，不阻断，正常走后续流程 |
| 🟡少见 | 占比>30% 且 机构数≤4 | 提示"研究领域较细、针对性较高"，正常推荐 |
| 🟡少见 | 占比>30% 且 机构数>4 | 判定过于分散，不分档、不推荐 |
| ✅桥梁 | 占比>50% 且 机构数≤4 | 提示"该作者倾向交叉学科研究"，正常推荐 |
| ✅桥梁 | 占比>50% 且 机构数>4 | 判定过于分散，不分档、不推荐 |

优先级：不成立>少见>桥梁，只报最先触发的一条；"不成立=1篇"的排除生效后，
仍会继续往下检查少见/桥梁（不是排除完就直接跳过）。少见/桥梁两个比例判断
都要求窗口A匹配JCR的论文总数≥4篇才评估，不成立分支的数量判断不受这条限制。

**排除的范围**：不成立分支排除1篇论文时，是把这篇论文所在的**整个学科桶**在
窗口A（分档判断）和窗口B（代表作推荐）里都当作不存在处理，不是只排除这一篇——
避免"这篇论文被判定不可信，换个窗口又被采信"这种自相矛盾。

### 架构调整

研究方向信号的计算（`resolve_domain_signal`）挪到了分档（`classify_professor`）
之前统一执行一次，不再是分档函数内部的一个附加步骤——因为它的"排除"这个结果
要同时影响窗口A和窗口B两处，必须在两者都还没处理之前就决定好，不能事后修补。
`classify_professor`因此变回了纯粹的①②③④决策树，不再需要感知研究方向信号。

新增配置校验：如果`--window-b-since`比`--window-a-since`还早，直接报错并说明
原因（这种配置会让"排除"逻辑的前提假设不成立），不会静默地按错误配置跑下去。

### 清理的旧代码

删掉了`BRIDGE_BUCKETS`（单一固定集合，被按主领域动态判断的`bucket_relationship`
取代）、旧的`assess_domain_mix`函数、`MIN_BUCKET_PAPERS_TO_COUNT`等旧常数、以及
`classify_professor`里专门为研究方向信号写的`_finish`包装器。

### 版本记录搬家

这份文件本身就是这次改动的一部分——之前notebook里那个越滚越长的"更新说明"
markdown cell（从v1一路记到v19，一万七千多字符）现在搬到了这里，notebook里
只保留一个指向这份文件的简短说明和实际要跑的代码。

---
## v21 更新：MEDICIN关键词漏了MEDICAL词根，两个真实类目一直没分到桶里

用户贴出一位教授的"判断依据引用论文"截图，追问"BMC Medical Imaging + Computer-
Aided Design + BMC Medical Informatics and Decision Making"这个组合算不算污染。
查证后：这三本期刊分别落在医学/临床和计算机科学两个大类，两者是bridge（家族C×B
默认桥梁关系）——不算污染，判断正确（这三篇论文的主题本身就是"深度学习/AI方法
用于医学影像分析"，医学影像期刊+计算机期刊的组合是这个研究方向的正常发表模式）。

但核查过程中顺带发现一个真实的分桶漏洞：`MEDICIN`这个关键词是为了同时匹配
`MEDICINE`和`MEDICINAL`，但匹配不到`MEDICAL`这个不同词根，导致`MEDICAL
INFORMATICS`、`MEDICAL LABORATORY TECHNOLOGY`这两个真实JCR类目一直没有被
分到任何大类里（论文因此完全不参与研究方向信号的计算，既不会被算作偏离也不会
被算作正常，是彻底的"消失"，不是"被判定为无害"）。加上`MEDICAL`关键词后修复，
全量254个类目重新核对过，只多分对了这两个之前漏掉的类目，没有引入新的误判。

副作用（保留，未特殊处理）：`MEDICAL ETHICS`原来因为落不到医学桶，退而靠`ETHICS`
关键词分进了人文桶；现在因为医学桶在列表里排得更靠前、又能匹配`MEDICAL`，改分到
了医学/临床。医学伦理学归医学还是归人文本身就没有标准答案，这次没有特殊处理保留
旧结果，如果后续觉得不合适可以再调整。

---
## v22 更新：小样本试跑里，身份核对和期刊分档的打印合并成一个循环

之前小样本试跑阶段，身份核对结果和期刊分档预览是两个独立的循环，各自把10个人
过一遍——同一个人的姓名/机构会出现两次，看着比较冗余。这次改成一个循环：
先跑期刊分档（如果上传过JCR），然后一次遍历里，先显示这个人的身份核对结果，
紧接着（如果身份是ok、且跑过期刊分档）显示期刊分档结果，同一个人只出现一次。

没上传JCR名单时，这个合并后的循环会自动降级成只显示身份核对结果，不影响
原有的"跳过期刊分档"这条路径。

同时确认了一个用户提出的疑问：`FIELD_MAJOR`（比如"Business, Finance"）确实被
研究方向信号的"主领域"判断使用（`resolve_domain_signal`里的`field_hint`参数），
不是没考虑——只是有个前提：如果这位教授窗口A匹配JCR成功的论文里，压根没有
一篇落在`FIELD_MAJOR`对应的学科大类里，没法锚定到一个空桶，会退回按论文数量
投票决定主领域，这是设计上有意为之的兜底，不是bug。

---
## v23 更新：两个真bug——中文姓名词序反过来时，会误选到完全不相关的同名人

用户实际发现的案例：名单上"Assoc Prof Wei Zhang (NTU)"（金融教授），代码匹配到的
却是另一个真实存在、做医学影像/人脸识别的"Wei Zhang"——用户在OpenAlex网站上用同样
的姓名+机构筛选，只搜出唯一一个人，而且明确是金融方向，说明真正对得上的候选人
其实存在、也能被搜到，问题出在代码的候选人筛选环节。用一个诊断脚本（直接查目标ID
本身信息、重放代码的机构过滤查询、纯姓名不限机构搜索）逐层定位，找到了两个独立的
真bug，叠加在一起导致了这次误判：

**bug 1：`institution_match_level()`的±1年容差被不相关的机构"顺带"触发。**
这个容差本来是为"这个人历史机构就两三个、刚发生过一次平稳过渡"这种简单场景设计的——
比如从A校跳到B校，B校最新一篇论文可能因为数据延迟被标成A校那年。但实现上是拿目标
机构自己的年份，去和这个人**全部**机构历史里的最大年份比较——对于历史机构又多又杂
的档案（这次的例子里，被误选的人历史上待过5个互不相关的机构），目标机构的唯一年份
可能纯属巧合地落在另一个八竿子打不着的机构的最新年份1年以内，两者之间根本没有真实
的"转校"关系。而档案历史越分散，本身恰恰越像是身份被OpenAlex错误合并的信号，这种
情况下容差应该更严格，不是和干净档案一样宽松。

**bug 2：`name_matches()`对中文姓名词序完全不敏感，是真正的病根。**
`name_variants()`本来就会同时生成"姓在前"和"名在前"两种姓名变体去搜索，专门用来
应对这种词序歧义；紧跟着的`name_compatible(variant, 候选人姓名)`也确实是按每一种
变体分别检查的，能正确处理。但`name_matches(原始姓名, 候选人姓名)`这个检查用的是
**没有拆分成变体的原始姓名字符串**，"最后一个词当作姓"这个假设是死的，不会跟着
变体换。名单上写的是"Wei Zhang"（姓在前），真正金融教授的OpenAlex显示名是
"Zhang Wei"（名在前，字面上是同两个词换了个顺序）——这个检查把名单的姓算成了
"zhang"，把候选人的姓算成了"wei"，判定"姓对不上"，直接把正确候选人拒之门外；
而那个被误选的人，OpenAlex显示名恰好也是"Wei Zhang"（和名单原始字符串词序一样），
反而顺利通过了这个检查。

**修复**：①`institution_match_level`的容差改成只在历史机构≤2个时生效，超过2个
必须精确匹配到这个人自己历史里的最新年份。②`name_matches`整个删掉（连带它唯一的
调用方——之前只服务于它的`_split_name`一起清理），因为`name_compatible`本身
（按变体逐一检查）已经能正确、完整地提供同等保护，不需要再叠加一个对词序不敏感、
反而会误杀的重复检查。

用真实数据完整复现过：修复前，`find_author()`唯一选中错误的那个人、还标成最高
置信度的"verified"；修复后，唯一选中真正的金融教授，同样是"verified"。另外单独
验证过`name_matches`删掉之后，`name_compatible`本身依然能正确拒绝"同姓不同名"
的情况（比如搜"Ka Yan Lee"不会被误认成"Ka Wing Lee"），确认这次修复没有削弱原有
的保护，只是去掉了一个对词序视而不见、反而会帮倒忙的重复检查。

---
## v24 更新：即使姓名+机构收窄到唯一候选人，也加一道"领域对不对得上"的兜底

上一版修复了两个具体bug（机构时效性容差被误触发、姓名匹配对中文词序不敏感），
但用户接着追问了一个更根本的问题：**就算这两个bug修好了，姓名+机构匹配本身就
没法保证唯一候选人一定是对的人**——两种词序都试过，也没法排除"两个不同的人，
姓和名恰好互换"这种巧合（不是同一个人被记录了两次，是两个真实存在的不同人，
名字正好是对方的姓名倒过来）。

这次加了一道独立的兜底：**即使机构+姓名已经收窄到只剩一个候选人，只要配置里填了
`FIELD`，也会核对一下这个候选人的研究主题跟`FIELD`搭不搭**——之前`field_score`
这个领域打分只在"有多个候选人需要二选一"时才会用到，如果姓名+机构已经（哪怕是
错误地）收窄到唯一候选人，代码会在见到这个领域打分之前就直接返回了，这个独立
信号根本没机会派上用场。现在只要唯一候选人的研究主题跟`FIELD`完全不沾边（比如
`FIELD="finance"`，候选人主题全是医学影像/人脸识别），就直接停下来、标注成新的
`needs_review_field_mismatch`状态交给人工核实，不再继续尝试后续任何候选人筛选
逻辑——因为"唯一候选人"这个前提本身可能就是匹配环节出了错，不该在错误的基础上
继续加工。

`batch_enrich.py`把这个新状态路由到独立的`enrich_status`（不会被并入"ok"）；
`confidence.py`把它按和`not_found`同等的不可信等级打分；`merge_to_excel.py`的
汇总打印新增了一个专门的计数（"唯一候选人但领域对不上 N 行"），跟已有的"疑似
OpenAlex身份污染"计数并列展示。

用构造数据验证过：领域完全不沾边时正确停下并标注、候选人的实际研究主题会附在
理由里；没填`FIELD`时不会误触发这条新检查；领域真的对得上时不会被误伤。完整跑
了一遍`batch_enrich.py`→`confidence.py`→`merge_to_excel.py`全链路，确认新状态
全程没有被静默并入"ok"。

---
## v25 更新：人工核实结果能回填了，不用再靠手动改Excel

之前的问题：Excel里标出了"需要人工核实"的人，但核实完之后没有地方"喂回去"——
只能自己手动改Excel的其他列，改完这条记录跟自动匹配成功的记录在数据结构上也不是
一回事（没走过分档、没有代表作推荐）。

**新增`merge_to_excel.py`里的"人工核实结果"列**（黄色底色，跟其他OA_/JR_输出列
区分开，表头带批注说明格式）——填三种格式之一：
- 一个OpenAlex作者ID（如`A5102221172`）= 确认是这个人
- `ok` = 确认"OA_候选人"列里列出的唯一候选人是对的
- `skip` = 确认在OpenAlex上找不到，以后不用再提示

**新脚本`apply_manual_review.py`**：读取填好的Excel，重新抓取确认的候选人完整
信息（论文、机构、主题等，跟`batch_enrich.py`自动匹配时抓的是同一套），输出
一份更新后的`enriched_reviewed.json`——人工核实过的记录跟自动匹配成功的记录
数据结构完全一样，能正常走后续的分档和代表作推荐，不用特殊处理。

`notebook`里加了第14步：下载Excel、人工核实、重新上传、跑这一格，然后回到
第9-11步重新跑一遍分档/可信度/生成Excel即可。

**明确不做的事**：不会帮忙绕过学校官网的反爬虫检验去自动抓取个人主页信息——
这类防护是网站明确不希望被自动化程序批量访问，合法的路子是人工打开网页看，
把有用的内容（包括Semantic Scholar这类第三方站点上的信息）复制粘贴过来，
不需要API也不需要绕过任何东西。

用构造数据完整测试过：OpenAlex ID直接确认、"ok"确认唯一候选人（含"填了ok但
候选人不止1个"这种应该被拒绝而不是瞎猜的边界情况）、"skip"标记跳过、格式无法
识别时原样跳过并在汇总里列出。人工确认过的记录重新跑`journal_ranking.py`，
分档结果正常产出，确认跟自动匹配的记录处理方式完全一致。

---
## v26 更新：连续好几个人都api_error时，整体暂停冷却一下，不再每个人都硬撑重试

用户实际观察到的现象：全量跑的时候，连续6个人清一色全是`api_error`。之前的
"降级"机制（同一个人的多次请求之间，一次失败后不再重复整套重试）只在**同一个
人自己**的请求之间生效——换到下一个人，又是从头开始整套重试再失败一次，纯粹
在硬撑，完全不知道要撑多久，也没有对"这看起来是系统性问题，不是单个人偶发的
网络抖动"这个信号做任何反应。

这次加了一个跨人的熔断机制：连续N个人（默认3个）都是`api_error`（或者更泛化的
`error`，因为这类异常也可能是网络问题漏网导致的），就整体暂停一段时间
（默认30秒）再继续，而不是每个人都从头硬撑一遍重试。如果冷却完还是连续失败，
下一次冷却时间翻倍（有上限，默认5分钟）；只要中间有一次不是api_error（哪怕是
`not_found`这种正常的"查完了但没匹配上"），说明API本身在正常响应，立刻重置
计数器和冷却时间。

用mock测试验证过：连续6人全部api_error、阈值设成3，触发了两次冷却，第二次
冷却时间正确翻倍（2秒→4秒）；构造了"前几个人不是api_error、后面才开始连续
失败"的场景，确认计数器只从真正开始失败的那个人算起，不会被之前的正常结果
污染计数。

之前v16那次"文件完整性排查"提到过磁盘上出现过一份类似逻辑、但因为找不到对应
操作记录而被撤掉的代码——这次是重新独立设计、写清楚了触发条件和参数、并且
完整测试过的版本，不是把那份来源不明的代码原样捡回来。

---
## v27 更新：找到了api_error真正的根源——OpenAlex从2026年2月起强制要求API Key

用户反馈"熔断冷却的等待时间明显在增长，但完全没有改善api_error的情况"，这句话
是关键线索——如果真的是限流问题，等待时间变长应该会有改善；完全没有改善，说明
从一开始就假设错了这是个"等等就好"的问题。

查了OpenAlex官方changelog和博客，确认：**OpenAlex从2026年2月13日起把API Key
从"推荐"变成了"强制要求"**，之前那套只靠`mailto`参数的"礼貌池"机制已经正式
废弃。不带Key的话，每天只有大约100次免费测试额度，用完之后所有请求持续失败——
**不管等多久都不会恢复**，因为这不是"请求发得太快"的问题，是"没有有效凭证"的
问题，二者需要完全不同的应对方式。这套代码是在这次变更之前写的，之前的重试/
熔断冷却机制建立在"失败是暂时的、等一等就好"这个假设上，对"credit用完了"这种
情况完全无效——261人的名单，每人要发5-15次请求，跑到第10-20个人左右额度就会
耗尽，之后全部失败，跟用户截图里的现象完全对得上。

**修复**：给`resolve_v2.py`和`fetch_openalex.py`（两边各自独立的`_get()`函数）
都加了一个模块级的`API_KEY`变量，设置一次、自动带进每次请求——没有像`mailto`
那样逐个给几十个函数加新参数（那样改动面太大、容易漏改）。所有会调用OpenAlex
API的脚本（`batch_enrich.py`、`merge_to_excel.py`、`openalex_links.py`、
`resolve_identity.py`、`reverse_lookup.py`、`apply_manual_review.py`、
`fetch_openalex.py`自己的命令行）都加了`--api-key`参数。notebook配置区新增
`API_KEY`这一格（放在`EMAIL`上面，因为现在真正决定"请求能不能成功"的是这个，
不是邮箱），不填的话会打印醒目警告；所有原来带`--mailto EMAIL`的命令都同步
加上了`--api-key API_KEY`。

**获取方法**：`openalex.org`免费注册账号，`openalex.org/settings/api`复制Key，
全程不到1分钟，目前对绝大多数用户依然完全免费。

用mock请求验证过：`api_key`确实被正确带进了`resolve_v2._get()`和
`fetch_openalex._get()`两边各自独立发出的请求参数里；`batch_enrich.py
--api-key`确实把值设置到了两个模块上。这次也从notebook文件本身重新提取脚本、
直接对notebook的实际内容做了验证，不是只测了磁盘上的脚本文件。

---
## v28 更新：三处独立的机构比对逻辑，都把"没法确认"误判成了"确认不一致"

用户贴出的截图显示："标记为ok但机构仍对不上：66位"，逐条看都是"NUS"对
"National University of Singapore"这种——明明是同一个机构，缩写和全称对不上
纯粹是字符串层面没做展开。查下来发现这个投影里**有三处独立实现**在比较"名单
机构缩写"和"OpenAlex机构全称"，三处都有同一类问题：机构缩写没法展开时（本地
别名缓存里没有、又刚好没有API额度去现查——正是v27那次额度耗尽的连锁反应），
都会退化成"缩写字符串是不是全称的子串"这种检查——但"nus"永远不会是
"national university of singapore"的子串，这个退化路径对纯缩写来说**必然是
错的**，和真的机构不一致没法区分。

三处分别是：
1. **notebook里跑完batch_enrich之后的预览cell**——最粗糙的一个，压根没尝试
   展开缩写，直接拿"名单机构第一个词"去匹配。
2. **`merge_to_excel.py`的`_institution_mismatch()`**——有认真尝试展开（先查
   本地别名表，再尝试联网解析），但展开失败时会退化成上面那种子串检查，返回
   一个布尔值，把"没法确认"和"确认不一致"混成了同一个`True`。
3. **`confidence.py`的`_inst_agrees()`**——这个脚本设计上不做联网调用，只查
   本地别名表，缩写没缓存时同样退化成子串检查，还会因此扣15分、打上
   `institution_mismatch`标记，把本来靠谱的匹配拖低到"需要人工核实"这一档。

**修复**：三处都改成区分"确认一致/确认不一致/没法确认"三种状态，只有"确认
不一致"才会真的标警告、扣分；"没法确认"单独标注、给一句"不代表真的不一致"
的说明，`confidence.py`里也把扣分从15分降到2分。notebook的预览cell改成直接
复用`resolve_v2.py`同一套别名解析逻辑（和最终生成Excel时用的是同一套），
不再自己另搞一套简化版判断。

用"NUS"/"National University of Singapore"这个真实案例在三处分别复现过
修复前的误判、验证过修复后能正确区分"没法确认"和"真的不一致"，也验证过
真实的机构不一致（比如名单写HKU、记录显示的是完全不相关的Stanford）依然能
被正确抓出来——这次的修复是收窄误报，不是把真正的问题也一起放过去。

---
## v29 更新：漏备份的第二份缓存文件 + 减少一半冗余查询

用户上传了他备份的`enriched.json`和`openalex_cache.json`，追问"这些明明是额度
用尽之前搜索过的，为什么还会显示需要核查"。查了上传的这两个文件确认：**这个
项目其实维护着两份完全独立的缓存**——`.openalex_cache.json`（存"这个人是谁、
论文有哪些"）和`assets/institution_aliases.json`（专门存"NUS"="National
University of Singapore"这种机构缩写的解析结果）。用户备份、重新上传的是第一份，
**第二份完全没有备份**。查了Prof. A在他上传的`enriched.json`里的记录，
身份数据完全正确、完整（`effective_institutions`正确显示为NUS的全称）——问题
从来不是身份查错了，是"机构对不上"这个核查需要单独解析"NUS"这个缩写，而这份
解析结果存在另一份没被备份的文件里，新session里只有内置的种子缩写表（十几个
常见的港新机构），用户跑的这份名单里其他机构就都得重新联网解析，而额度还没恢复。

**修复一**：把这份机构缩写缓存也纳入备份/恢复流程——notebook下载步骤现在会
一并下载`assets/institution_aliases.json`；写入种子缩写表那格后面新增一个
可选步骤，可以上传之前备份的这两份文件接着用，并在说明里明确写清楚"这两份
文件缺一不可，只备份一份的话机构核查还是会查不清楚"。

**修复二（用户明确要求的"减少查询量"）**：`_find_author_inner()`里给每个姓名
变体做机构过滤搜索时，之前固定发两次查询（`affiliations.institution.id`和
`last_known_institutions.id`）——但OpenAlex返回的作者记录本身就带着
`last_known_institutions`这个字段，不管是被哪个filter找到的，第一次查询的结果
就足够算出"是不是当前机构"，完全不需要专门再发一次查询去拿本来就有的数据。
改成默认只发第一次查询，只有这次查询一无所获时才补第二次兜底（防止极少数
"last_known比affiliations历史更新"的边界情况被漏掉）。常见情况下，机构搜索
这一步的查询量能砍掉接近一半。

用mock请求验证过：正常情况下（第一次查询已经找到结果）第二次查询被正确跳过；
构造了"只有last_known_institutions.id能找到人"的边界场景，确认兜底逻辑依然
生效、不会漏掉这个人。重跑了全部已有的回归测试（api_error、not_found、Jiang
Luo姓名词序反转案例、同姓不同名保护），确认这次改动没有引入新的问题——过程中
其实抓到了一个自己重构时引入的真bug（嵌套函数里少声明了一个`nonlocal`变量，
导致`UnboundLocalError`），也是靠这轮回归测试在交付前发现并修好的。

---
## v30 更新：JCR表格里的空字符串百分位，会让整个期刊分档直接崩溃

用户贴出报错截图：`journal_ranking.py`跑到一半直接崩了，`ValueError: could not
convert string to float: ''`。查下来是`load_jcr()`读取JCR表格"JIF百分位"这一列
的时候，完全没做任何类型校验，原样存了下来——大部分单元格是正常数字，但真实
JCR表格里有些行这一列是**空字符串**（不是真正的空值，是长度为0的字符串），
`effective_percentile()`处理这种"没有学科分类详情、只能退回顶层JIF百分位"的
期刊时，直接对这个值调用`float()`，遇到空字符串就崩了——不是某个人处理失败、
记个日志跳过，是**整个批次直接停止**，连`ranked.json`都不会生成，前面已经跑成功
的人也一起白跑。

**修复**：加了一个`_to_float()`安全转换函数，任何转换失败（空字符串、非数字
文本、None）都返回`None`，不会抛异常。在`load_jcr()`读取原始Excel数据的源头
就用这个函数处理"JIF百分位"和"影响因子JIF"这两列，不需要每个用到这两个字段
的下游代码各自防御。`_parse_category_detail()`那边本来就是安全的（只从正则
表达式捕获到的、保证是数字的rank/total算百分位，格式不对的行直接跳过，不会
硬解析出脏数据），这次不用动。

用构造的JCR数据复现了崩溃场景、确认修复后不再崩溃，改成正确返回"没有百分位
数据"、进而正常归到④默认档，不会被误算成强或弱的信号。用真实的22643行JCR
文件重新加载了一遍，统计出**2306行**都是这种"JIF百分位是空、又没有学科分类
详情"的情况——差不多每10行就有1行是这个数据形状，说明这次崩溃对任何覆盖面
稍广一点的名单来说基本是必然会踩到的，不是小概率的边缘情况。

---
## v31 更新：第12步"待人工确认.md"一直没被真正生成过

用户跑第12步（生成人工核对清单）看起来一切正常、正常打印出了内容，但紧接着
下载那一格报`FileNotFoundError: Cannot find file: 待人工确认.md`。查下来是
`openalex_links.py`这个脚本本身设计成只往屏幕`print()`，要靠命令行`>`重定向
才能存成文件——但notebook里跑它用的是`run_streaming(cmd)`，这个函数只负责
实时打印，打印完就丢了，从来没真正写过文件。这是很早之前把所有脚本调用统一
改成`run_streaming`（为了解决全量跑几小时看起来卡住的问题）时，不小心引入
的一个回归——当时其他脚本都有`--out`参数自己直接写文件，没受影响，唯独
`openalex_links.py`是靠标准输出重定向设计的，被波及了但一直没发现，因为
跑那一格本身不会报错，只是悄悄没生成文件，要等到下一格下载的时候才会暴露。

**修复**：给`run_streaming`加了一个可选的`save_to`参数——传了文件路径的话，
会一边实时打印、一边把完整输出也存一份到这个文件。其他所有调用`run_streaming`
的地方都不受影响（不传这个参数，行为和原来完全一样），只有第12步这一格改成
传入`save_to="待人工确认.md"`，把这个文件真正补回来。

用构造数据完整测过：跑完之后`待人工确认.md`确实生成了，内容和屏幕上打印的
一致。也检查了notebook里其他7处调用`run_streaming`的地方，确认都是脚本自己
用`--out`参数直接写文件，不依赖标准输出捕获——只有`openalex_links.py`这一处
受这次回归影响，不是notebook里普遍存在的问题。

---
## v32 更新：openalex_links.py 脚本自己也补上了 --out 参数

v31修的是notebook这一侧（`run_streaming`加了`save_to`参数，实时打印的同时把
输出存成文件）。这次顺手把`openalex_links.py`脚本自己也补上了同样的能力——
加一个`--out`参数，脚本自己负责把内容写进文件，不再只是单纯`print()`、指望
调用方用shell的`>`重定向来存文件。两边修复不冲突：notebook里继续用
`run_streaming(..., save_to=...)`这套；如果有人在notebook之外、直接用命令行
跑这个脚本，现在也能用`--out 文件名`直接生成文件，不用依赖shell重定向。

用构造数据验证过：加了`--out`参数之后，文件正确生成，内容和同时打印到屏幕上
的一致。

---
## v33 更新：Excel里11列数据整体错位——目前为止最严重的一个bug

用户上传了实际跑出来的Excel和待人工确认.md，想核对具体内容，结果一逐列核对
发现"JR_研究方向偏移提示"这一列显示的是个孤零零的数字（比如"3"），根本不是
应有的文字说明。查下来是**从"OA_合并记录说明"一直到"JR_代表作预览"，一共11列
的数据全部整体错位了一格**——每一份用最近几版脚本生成的Excel都有这个问题，
不是这一次运行的偶发情况。

**根源**：`merge_to_excel.py`用两个平行的列表拼Excel——`NEW_COLS`（表头）和
`vals`（每一行的具体值），两个列表靠位置一一对应。之前加"人工核实结果"这个
输入列的时候，只把它加进了`NEW_COLS`表头列表，**忘了在`vals`这个手写的数值
列表里补一个对应的占位值**——这个bug完全不会报错、不会有任何警告，Excel照样
正常生成、每个格子里都有"看起来合理"的内容，只是内容全部串到了错误的表头
下面，肉眼扫一眼很难发现，必须真的核对"这一列的标题和这一列的实际内容对不
对得上"才能看出来。

**修复**：在`vals`列表里补上了这个漏掉的占位值（空字符串，因为这一列本来就
不该由脚本写任何内容，是留给用户填的）。

**这次验证方式升级了**——不再只是孤立地测单个函数（`_institution_mismatch`、
`_inst_agrees`这些之前都各自测过、也确实都是对的），而是**真正跑一遍完整的
四段式流程**（`batch_enrich.py`→`journal_ranking.py`→`confidence.py`→
`merge_to_excel.py`，用逼真的mock数据模拟一条完全成功匹配的记录），然后把
生成的Excel里全部27个脚本写入列，逐列打印表头和实际值核对——确认每一列的
内容类型都跟表头描述的一致（比如"JR_学术分档"是"①高质量期刊作者"这种短标签，
不是一整段推理文字；"OA_可信度分"是个整数，不是等级字符串）。这种"两个独立
维护的列表对不齐"的bug，单独测每个函数是测不出来的——只有真正跑一遍、检查
最终产出物，才能发现。

---
## v34 更新：按用户要求精简Excel列，同时补完一次半成品的重构

用户明确列出了只想保留的7类内容：任教起始时间、近年论文清单、研究主题、
被引/发表总数（含新增的"近年被引数"）、简化成三态的研究方向偏移提示（未偏离/
研究领域较细/交叉学科研究）、代表作拆成15列（3篇×标题/合著者/年份/期刊/学科
排名，按年份从近到远排列）、可信度按1-4打分（1最好），并要求把这些规则写进
"颜色图例"表的备注里。

**排查发现代码里已经有一次不完整的重构**——`NEW_COLS`（表头列表）已经被改成
了精简后的新结构，但`_write_roster_sheet()`里真正往表格填数据的`vals`列表
完全没跟着改，还是老版本21+项内容，跟新表头完全对不上（这次不是像上一版那样
"错位一格"，是结构性的彻底不匹配）。

**这次做完整**：
- 重写`vals`构造逻辑，跟精简后的`NEW_COLS`一一对应
- 新增`_recent_citations()`：从OpenAlex的`counts_by_year`逐年数据里，把
  `--window-a-since`起点之后的被引数加总，得到"近年被引数"（区别于历史总被引数）
- 新增`_confidence_1_to_4()`：把原有的"高/中/低/不可用"四档可信度，直接映射成
  1-4分（1=高=最好）
- 新增`_domain_deviation_label()`：把`resolve_domain_signal()`内部更丰富的
  状态，收窄成用户要求的固定三种取值
- 新增`_rep_paper_columns()`：最多3篇代表作按年份从近到远排序，拆成
  标题/合著者/年份/期刊名称/学科排名（JCR"学科内排名/总数"格式，如"9/243"）
  共15列，不足3篇的用空值补齐
- 补上了之前只停留在注释里、从没真正写出来的"把规则写进颜色图例表备注"这部分

用真实端到端流程测过（2篇代表作，一篇带合著者、一篇不带，构造了有实际数值的
逐年引用数据）：近年被引数正确加总、代表作按年份新到旧正确排序、合著者只在
有数据的那篇正确提取、学科排名格式正确、颜色图例表的新增说明正确追加在原有
内容后面而不是覆盖掉。

---
## v35 更新：新增导师推荐模块 advisor_recommend.py（半成品，作为独立可展示模块）

之前的流程停在"给每条记录打可信度分、打期刊分级"——把结果整理成"到底该推荐
哪几位导师"仍然要用户自己对着 Excel 手动判断。这一版加了 `advisor_recommend.py`，
把 `confidence.py` / `journal_ranking.py` 已经算出来、已经写明理由的信号组合成
一份可直接用的名单：一张"不推荐名单"（列出具体排除原因），一张"推荐排名"表
（综合得分从高到低排序，Top-N 高亮）。

**设计上刻意没有引入新的、不可解释的判断**：排除规则完全复用已有字段
（`reliability.level`、`profile_contamination_risk` 标记、`journal_ranking.tier`
里的 `not_recommended`/`contaminated`），推荐得分是三个已有信号的加权和
（身份可信度 40% + 期刊分级 40% + 近期是否有代表作 20%），每一条"推荐理由"
都是把上游脚本已经生成的文字拼接起来，不生成新的评价性语言——跟项目一贯的
"不猜测、每个结论都要能追溯到具体证据"原则保持一致，细节写在新增的
`references/advisor-recommendation-design.md` 里。

**这次是作为独立展示模块开发的**，还没有接入真实的每日自动监控和网页表单
（这两块在设计文档最后一节明确写清楚了"尚未实现"，避免文档过度承诺）。构造了
一份 15 人的合成测试数据（`examples/sample_scored.json`，覆盖：正常高分、无
期刊分级数据、身份可信度低、身份污染标记、期刊分级为"不推荐"、研究方向组合
判定为"contaminated"共 5 种排除路径），实际跑了一遍脚本验证：5 位应排除的
全部正确进入不推荐名单且各自原因文字对得上触发条件，剩余 10 位按预期公式
算出的分数排序，Top-N 高亮列正确标记，两张 sheet 的表头和列宽都能正常打开。
产出的示例文件存在 `examples/advisors_example.xlsx`。

---
## v36 更新：修正 advisor_recommend.py 的推荐逻辑（v35 版本设计有误，本次重做）

用户看完 v35 版本后指出一个概念性问题：v35 把"这条记录本身能不能信"（身份核
验层面，`confidence.py` 早就有的可信度概念）和"这个导师适不适合推荐给申请者"
（研究方向匹配层面，用户这次真正要的东西）混在了一起——v35 的"不推荐名单"
里，身份可信度低的记录和研究方向不符的记录被算作同一类原因，导致"不推荐"
这个标签本身语义不清。用户举了个具体例子：申请者方向是公司金融，但对应导师
近两年研究其实是农业经济，这种情况才应该叫"不推荐"，而不是身份不确定。

**这次重新设计，拆成三份清单而不是两份**：
- **待人工核实身份**——身份可信度低/污染风险/研究方向内部自相矛盾（原来
  `journal_ranking.py` 的 `contaminated` 分档，本质是提示"这个 OpenAlex ID
  可能混入了另一个同名人"，属于身份问题，不是方向问题，这一版把它从"不推荐"
  移到了这里）。这类记录完全不参与后面的推荐/排除判断——身份都没确认，无法
  对"是否适合"给出任何有意义的结论。
- **不推荐**——身份没问题，但方向或产出不达标：近期论文的研究方向关键词
  完全对不上申请者兴趣、或近期完全没有论文产出。
- **推荐排名**——剩下的人按「方向匹配度50% + 期刊等级30% + 发表密集程度20%」
  加权排序，Top-N 高亮。

**方向匹配度算法也一并重做了**：用户提出用户点出的另一个问题——单纯按关键词
是否出现在论文标题里判断"方向匹配"太粗糙，很多论文标题是某个细分问题的具体
表述，不会重复整个学科名。改成匹配 OpenAlex 自己给每篇论文打的主题分类
（topic/subfield/field 三级标签，一套约4500个主题、约250个子领域、26个大
学科的固定分类体系），命中层级越细分权重越高（topic命中=1.0，subfield=0.7，
field=0.35），只有在论文完全没有主题分类数据时才退回到标题关键词粗匹配（权重
0.5）。为此顺手改了 `fetch_openalex.py`：在原有的 `topics`（字符串列表，其他
脚本仍在用，完全没动）基础上，新增了 `topics_detail` 字段保留 OpenAlex 原本
就返回、但之前被丢弃的 subfield/field 信息——纯增量修改，不影响任何已有脚本。

**这次也修了一个开发过程中自己发现的 bug**：第一版实现里，`recommend_score()`
计算"发表密集程度"时，对没跑过 `journal_ranking.py` 的记录，错误地把论文数
直接当 0 处理，而不是像排除判断那一处逻辑一样退回到 `recent_works` 的实际
长度——导致一位实际发了1篇论文的候选人在"推荐理由"里显示"发表0篇"。用真实
跑了一遍全部15条示例数据、逐条核对推荐理由文字与原始数据是否对得上才发现，
现改为两处统一从同一个已计算好的论文数变量取值，不再各自重复计算。

用扩充到17人的合成数据重新跑了一遍全流程（新增了两个"方向明显匹配但身份不
可信""身份记录内部自相矛盾"的专门样例，以及用户举的农业经济例子本身），确认
三份清单人数相加等于总人数、每条排除/推荐理由都能在原始数据里找到依据。
`references/advisor-recommendation-design.md` 也重写了，第6节额外如实记录了
一个局限性：当前仍是关键词子串匹配，不是语义匹配，示例数据里几位"方向相邻但
关键词没完全对上"的候选人（宏观金融、家庭金融、市场微观结构等）都被判了"方向
偏移"排除——这是子串匹配方法的固有局限，不是 bug，需要申请者提供足够覆盖面的
兴趣关键词列表来缓解，而不是靠算法自己判断"相关"。

---
## v37 更新：上传 GitHub 前的隐私/信息安全清理

用户明确要求上传 GitHub 前把所有 API 相关信息和个人信息清理干净，避免信息
泄漏或账号被滥用。全仓库做了一次系统性排查：

- **API key / 凭证**：确认没有任何脚本把 `--api-key`/`--mailto` 写死或缓存
  到文件里（每次运行都是命令行参数传入，用完即走），仓库里也没有任何真实
  key/token 格式的字符串——`API_KEY = None` 只是脚本内的默认值占位。
- **真实人名**：`references/pitfalls.md` 和 `CHANGELOG.md` 里此前用真实案例
  记录过几个 bug（姓名词序颠倒导致误匹配、机构别名缓存丢失导致误判等），
  案例里带着真实教授姓名（用于说明具体是哪次匹配出的错）。这些姓名本身虽然
  是可公开检索的学术身份，但连同"用户实际发现的案例""用户上传的
  enriched.json"这类措辞放在一起，等于公开暴露了本人真实在研究、核实的具体
  导师名单——这属于用户自己的科研/申请隐私，不应该出现在公开仓库里。已经
  全部替换成统一的占位姓名（技术细节、bug 复现逻辑完全保留，只是把真实人名
  换掉），`scripts/resolve_v2.py` 注释里同样性质的示例姓名也一并处理了。
- **`.gitignore` 补强**：新增了几类此前没覆盖到的、日常使用中容易被误提交
  的文件模式——用户自己的名单文件（`roster*.xlsx`）、脚本默认输出文件名
  （`result*.xlsx`、`advisors.xlsx`，`examples/` 目录下的示例文件用
  `!examples/*.xlsx`显式排除在忽略规则之外，不受影响）、以及一批防御性的
  凭证文件命名模式（`.env`、`api_key*.txt`、`credentials*.json` 等）——目前
  代码里没有任何脚本会生成这些文件，纯粹是防止以后本地顺手建了这类文件、又
  不小心 `git add -A` 提交上去。
- **`assets/institution_aliases.json`**：确认里面只有机构名缩写到全称的公开
  映射表，不含任何人员信息。

排查方法：对全仓库做了邮箱正则、常见 API key 格式（`sk-`前缀、`Bearer`token、
AWS风格key等）、连续大写人名模式的多轮 grep 扫描，逐条人工确认命中项是占位符
（如文档里的 `you@x.com`）还是真实敏感信息，只处理了后者，不影响任何示例/
占位文本。
